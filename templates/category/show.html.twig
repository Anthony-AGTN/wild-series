{% extends 'base.html.twig' %}

{% block title %}catégorie #{{ categories.id }}
{% endblock %}

{% block body %}
    <div class="media">
        <div class="media-body">
            <h1 class="mt-0">{{ categories.name }}</h1>
        </div>
    </div>

    <a href="{{ path('category_index') }}">
        Retour à la liste des catégories
    </a>
{% endblock %}
